- if signed_in?
  = image_tag(@user.profile_image_url).html_safe
  %h3 
    Welcome 
    = link_to(@user.name, @user.url)

  = form_tag logout_path do
    = submit_tag 'Sign out'

-else    
  = form_tag twitter_sign_in_path do
    = image_submit_tag("sign-in-with-twitter.png")


